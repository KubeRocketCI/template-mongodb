apiVersion: v1
kind: Service
metadata:
  name: mongodb-express
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8081
  selector:
    app.kubernetes.io/name: mongodb-express
  type: ClusterIP
